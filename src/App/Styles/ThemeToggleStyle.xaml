<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ThemeToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="MinWidth" Value="46"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Width" Value="46"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid Background="Transparent" Width="46" Height="24">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.2"/>
                                </VisualStateGroup.Transitions>
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="PointerOver"/>
                                <VisualState x:Name="Pressed"/>
                                <VisualState x:Name="Checked">
                                    <VisualState.Setters>
                                        <Setter Target="KnobTransform.X" Value="22"/>
                                        <Setter Target="SunIcon.Opacity" Value="0"/>
                                        <Setter Target="MoonIcon.Opacity" Value="1"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="CheckedPointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="KnobTransform.X" Value="22"/>
                                        <Setter Target="SunIcon.Opacity" Value="0"/>
                                        <Setter Target="MoonIcon.Opacity" Value="1"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="CheckedPressed">
                                    <VisualState.Setters>
                                        <Setter Target="KnobTransform.X" Value="22"/>
                                        <Setter Target="SunIcon.Opacity" Value="0"/>
                                        <Setter Target="MoonIcon.Opacity" Value="1"/>
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled"/>
                                <VisualState x:Name="CheckedDisabled">
                                    <VisualState.Setters>
                                        <Setter Target="KnobTransform.X" Value="22"/>
                                        <Setter Target="SunIcon.Opacity" Value="0"/>
                                        <Setter Target="MoonIcon.Opacity" Value="1"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <!-- Track -->
                        <Border CornerRadius="12"
                                Background="{ThemeResource AppBorderBrush}"
                                BorderBrush="{ThemeResource AppBorderBrush}"
                                BorderThickness="1"/>

                        <!-- Sliding knob with icon -->
                        <Grid Width="20" Height="20"
                              HorizontalAlignment="Left" VerticalAlignment="Center"
                              Margin="2,0,0,0">
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="KnobTransform" X="0"/>
                            </Grid.RenderTransform>
                            <Ellipse Width="20" Height="20"
                                     Fill="{ThemeResource SurfaceBrush}"
                                     Stroke="{ThemeResource AppBorderBrush}"
                                     StrokeThickness="1"/>
                            <FontIcon x:Name="SunIcon" Glyph="&#xE706;" FontSize="11"
                                      Foreground="{ThemeResource TextDimBrush}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <FontIcon x:Name="MoonIcon" Glyph="&#xE708;" FontSize="11"
                                      Foreground="{ThemeResource TextDimBrush}"
                                      Opacity="0"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
